namespace FountainOfObjects;

public class World
{
    private int Size { get; }
    // private int Room[,] Rooms { get; }

    public World(int size)
    {
        Size = size;
        // Rooms = new Room[size, size];
    }
    
}